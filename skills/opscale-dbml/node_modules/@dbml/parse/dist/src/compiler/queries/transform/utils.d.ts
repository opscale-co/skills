import { default as SymbolTable } from '../../../core/analyzer/symbol/symbolTable';
import { TableSymbol } from '../../../core/analyzer/symbol/symbols';
export type TableNameInput = string | {
    schema?: string;
    table: string;
};
/**
 * Normalizes a table name input to { schema, table } format.
 * Properly handles quoted identifiers with dots inside.
 */
export declare function normalizeTableName(input: TableNameInput): {
    schema: string;
    table: string;
};
/**
 * Looks up a table symbol from the symbol table.
 */
export declare function lookupTableSymbol(symbolTable: Readonly<SymbolTable>, schema: string, table: string): TableSymbol | null;
/**
 * Removes surrounding double quotes from a string if present.
 */
export declare function stripQuotes(str: string): string;
