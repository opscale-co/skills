import { SyntaxToken } from '../../../lexer/tokens';
import { ElementBinder } from '../types';
import { ElementDeclarationNode, ProgramNode } from '../../../parser/nodes';
import { CompileError } from '../../../errors';
import { default as SymbolFactory } from '../../symbol/factory';
export default class RecordsBinder implements ElementBinder {
    private symbolFactory;
    private declarationNode;
    private ast;
    private boundColumns;
    constructor(declarationNode: ElementDeclarationNode & {
        type: SyntaxToken;
    }, ast: ProgramNode, symbolFactory: SymbolFactory);
    bind(): CompileError[];
    private bindRecordsName;
    private bindTopLevelName;
    private bindInsideTableName;
    private bindBody;
    private bindDataRows;
    private bindDataRow;
    private bindSubElements;
}
