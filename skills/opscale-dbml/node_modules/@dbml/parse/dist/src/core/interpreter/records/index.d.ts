import { ElementDeclarationNode } from '../../parser/nodes';
import { default as Report } from '../../report';
import { InterpreterDatabase } from '../types';
export declare class RecordsInterpreter {
    private env;
    private tableToRecordMap;
    constructor(env: InterpreterDatabase);
    interpret(elements: ElementDeclarationNode[]): Report<void>;
    private validateConstraints;
}
