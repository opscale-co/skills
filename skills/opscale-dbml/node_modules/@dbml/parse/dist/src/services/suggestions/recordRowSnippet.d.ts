import { CompletionList, TextModel, Position } from '../types';
import { default as Compiler } from '../../compiler';
export declare function suggestRecordRowSnippet(compiler: Compiler, model: TextModel, position: Position, offset: number): CompletionList | null;
